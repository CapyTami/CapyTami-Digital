/* Cores neon e fontes */
:root {
    --neon-rosa: #FF10F0;
    --neon-azul: #00f3ff;
    --fundo-escuro: #1a1a1a;
    --fonte-titulo: 'Press Start 2P', cursive;
    --fonte-texto: 'Orbitron', sans-serif;
}

body {
    background-color: var(--fundo-escuro);
    color: white;
    font-family: var(--fonte-texto);
    line-height: 1.6;
    margin: 0;
    padding: 20px;
}

/* Cabeçalho estilo game */
.neon-header {
    text-align: center;
    padding: 50px 0;
    border-bottom: 3px solid var(--neon-rosa);
    margin-bottom: 30px;
}

.neon-header h1 {
    font-family: var(--fonte-titulo);
    color: var(--neon-rosa);
    text-shadow: 0 0 15px var(--neon-rosa);
    font-size: 2.5em;
    animation: neon-pulse 1.5s infinite;
}

/* Animação de brilho */
@keyframes neon-pulse {
    0% { opacity: 0.8; }
    50% { opacity: 1; text-shadow: 0 0 25px var(--neon-rosa); }
    100% { opacity: 0.8; }
}

/* Imagens com borda neon */
.neon-border {
    border: 3px solid var(--neon-rosa);
    border-radius: 15px;
    padding: 5px;
    box-shadow: 0 0 20px var(--neon-rosa);
    max-width: 200px;
}

/* Botões interativos */
.neon-button {
    background: none;
    border: 2px solid var(--neon-azul);
    color: var(--neon-azul);
    padding: 10px 25px;
    margin: 10px;
    text-decoration: none;
    font-size: 1.2em;
    transition: all 0.3s;
    display: inline-block;
}

.neon-button:hover {
    background: var(--neon-azul);
    color: var(--fundo-escuro);
    box-shadow: 0 0 25px var(--neon-azul);
}

/* Cards de projetos */
.game-card {
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
}

.neon-shadow {
    border-radius: 10px;
    transition: transform 0.3s;
    max-width: 300px;
}

.neon-shadow:hover {
    transform: scale(1.05
